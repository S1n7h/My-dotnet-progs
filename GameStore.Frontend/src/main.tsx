import React from "react";
import ReactDOM from "react-dom/client";
import App from "GameStore.Frontend/src/App.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);